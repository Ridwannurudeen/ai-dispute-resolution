{
  "name": "dispute-resolution-subgraph",
  "version": "1.0.0",
  "description": "The Graph subgraph for AI Dispute Resolution",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ dispute-resolution",
    "deploy:local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dispute-resolution",
    "create:local": "graph create --node http://localhost:8020/ dispute-resolution",
    "remove:local": "graph remove --node http://localhost:8020/ dispute-resolution",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.60.0",
    "@graphprotocol/graph-ts": "^0.31.0"
  },
  "devDependencies": {
    "matchstick-as": "^0.5.0"
  }
}
